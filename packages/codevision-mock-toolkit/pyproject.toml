[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "codevision-mock-toolkit"
version = "0.1.0"
description = "Mock LLM toolkit for CodeVision Module 8"
requires-python = ">=3.9"
